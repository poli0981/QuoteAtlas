/**
 * Consent version (docs/14 §1). Bump on any material change to the legal texts —
 * that re-gates every user (CLAUDE.md R11). The gate can never be bypassed,
 * feature-flagged off, or auto-accepted.
 */
export const LEGAL_VERSION = 1;
